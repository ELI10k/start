"use client";
import Link from "next/link";
import { notFound } from "next/navigation";
import { useMemo, useState } from "react";
import { useWorkouts } from "@/components/workouts/WorkoutProvider";
import type { AssignmentStatus } from "@/lib/workouts/types";
const labels:Record<AssignmentStatus,string>={active:"פעיל",paused:"מושהה",completed:"הושלם",archived:"בארכיון"};
export default function CoachWorkoutProgram({id}:{id:string}){const{getProgram,assign,snapshot,setAssignmentStatus}=useWorkouts();const program=getProgram(id);const clients=snapshot.clients;const today=new Date().toISOString().slice(0,10);const[clientId,setClientId]=useState("");const[startDate,setStartDate]=useState(today);const[endDate,setEndDate]=useState("");const[frequency,setFrequency]=useState(String(program?.trainingFrequency??""));const[note,setNote]=useState("");const[message,setMessage]=useState("");const[submitting,setSubmitting]=useState(false);const assignments=useMemo(()=>snapshot.assignments.filter((item)=>item.programId===id).sort((a,b)=>b.assignedAt.localeCompare(a.assignedAt)),[id,snapshot.assignments]);if(!program)notFound();const submit=async()=>{if(submitting)return;if(!clientId){setMessage("יש לבחור לקוח.");return}if(!frequency||Number(frequency)<1||Number(frequency)>7){setMessage("יש לבחור תדירות שבועית בין 1 ל־7.");return}if(endDate&&endDate<startDate){setMessage("תאריך הסיום לא יכול להיות מוקדם מתאריך ההתחלה.");return}setSubmitting(true);try{const ok=await assign({clientId,programId:program.id,startDate,endDate:endDate||undefined,weeklyFrequency:Number(frequency),coachNote:note||undefined});setMessage(ok?"התוכנית שויכה ונשמרה ב-Supabase.":"כבר קיים שיוך פעיל זהה או שהנתונים אינם תקינים.")}finally{setSubmitting(false)}};return <main className="px-4 py-8 sm:px-6"><div className="mx-auto max-w-5xl"><p className="text-xs font-bold text-[#D4AF37]">{program.official?"תוכנית רשמית לקריאה בלבד":"עותק מותאם"} · {program.sourceWorkbook}</p><h1 className="mt-2 text-3xl font-black">{program.name}</h1><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-4"><Summary label="ימי אימון" value={String(program.days.length)}/><Summary label="תרגילים" value={String(program.days.reduce((sum,day)=>sum+day.exercises.length,0))}/><Summary label="רמה" value={program.difficulty??"לא צוינה"}/><Summary label="תדירות" value={program.trainingFrequency?`${program.trainingFrequency} בשבוע`:"לא צוינה"}/></div><section className="mt-6 rounded-[24px] border border-[#3A321B] bg-[#17150F] p-5"><h2 className="font-black">שיוך ללקוח</h2><p className="mt-2 text-sm text-zinc-500">יש לעבור על ימי האימון לפני השיוך. שיוך חדש מחליף שיוך פעיל קודם ושומר אותו בהיסטוריה.</p><div className="mt-4 grid gap-3 sm:grid-cols-2"><label className="text-sm font-bold">לקוח<select className="nutrition-input mt-2" value={clientId} onChange={(event)=>{setClientId(event.target.value);setMessage("")}}><option value="">בחירת לקוח</option>{clients.map((client)=><option key={client.id} value={client.id}>{client.fullName}</option>)}</select></label><label className="text-sm font-bold">תדירות שבועית<input className="nutrition-input mt-2" type="number" min="1" max="7" value={frequency} onChange={(event)=>setFrequency(event.target.value)}/></label><label className="text-sm font-bold">תאריך התחלה<input className="nutrition-input mt-2" type="date" value={startDate} onChange={(event)=>setStartDate(event.target.value)}/></label><label className="text-sm font-bold">תאריך סיום (רשות)<input className="nutrition-input mt-2" type="date" min={startDate} value={endDate} onChange={(event)=>setEndDate(event.target.value)}/></label><label className="text-sm font-bold sm:col-span-2">הערת מאמן<textarea className="nutrition-input mt-2 min-h-24" value={note} onChange={(event)=>setNote(event.target.value)}/></label></div><button disabled={program.status!=="active"||submitting} onClick={submit} className="mt-4 min-h-12 rounded-2xl bg-[#D4AF37] px-5 font-black text-black disabled:opacity-40">{submitting?"משייכים…":"שיוך התוכנית"}</button>{message&&<p role="status" className={`mt-3 text-sm ${message.includes("נשמרה")?"text-emerald-400":"text-red-400"}`}>{message}</p>}</section><section className="mt-6"><h2 className="text-xl font-black">ימי התוכנית</h2><div className="mt-3 space-y-3">{[...program.days].sort((a,b)=>a.order-b.order).map((day)=><Link key={day.id} href={`/coach/workouts/${program.id}/days/${day.id}`} className="block rounded-[22px] border border-[#292929] bg-[#151515] p-5 hover:border-[#D4AF37]/50"><span className="text-xs text-zinc-500">יום {day.order+1}</span><h3 className="mt-1 text-xl font-black">{day.name}</h3><p className="mt-2 text-sm text-zinc-500">{day.exercises.length} תרגילים</p></Link>)}</div></section><section className="mt-6 rounded-[24px] border border-[#292929] bg-[#151515] p-5"><h2 className="text-xl font-black">היסטוריית שיוכים</h2>{assignments.length?<div className="mt-3 space-y-3">{assignments.map((assignment)=>{const client=clients.find((item)=>item.id===assignment.clientId);return <article key={assignment.id} className="rounded-2xl border border-[#292929] p-4"><div className="flex flex-wrap items-center justify-between gap-2"><div><strong>{client?.fullName??assignment.clientId}</strong><p className="mt-1 text-xs text-zinc-500">{assignment.startDate} — {assignment.endDate??"ללא תאריך סיום"} · {assignment.weeklyFrequency} בשבוע</p></div><span className="rounded-full border border-[#333] px-3 py-1 text-xs">{labels[assignment.status]}</span></div>{assignment.coachNote&&<p className="mt-3 text-sm text-zinc-400">{assignment.coachNote}</p>}<div className="mt-3 flex flex-wrap gap-2">{assignment.status==="active"&&<Action label="השהיה" onClick={()=>setAssignmentStatus(assignment.id,"paused")}/>} {assignment.status==="paused"&&<Action label="חידוש" onClick={()=>setAssignmentStatus(assignment.id,"active")}/>} {(assignment.status==="active"||assignment.status==="paused")&&<Action label="סיום" onClick={()=>setAssignmentStatus(assignment.id,"completed")}/>} {assignment.status!=="archived"&&<Action label="ארכיון" onClick={()=>setAssignmentStatus(assignment.id,"archived")}/>}</div></article>})}</div>:<p className="mt-3 text-sm text-zinc-500">אין שיוכים קודמים לתוכנית.</p>}</section></div></main>}
function Summary({label,value}:{label:string;value:string}){return <div className="rounded-2xl border border-[#292929] bg-[#151515] p-4"><span className="text-xs text-zinc-500">{label}</span><strong className="mt-1 block">{value}</strong></div>};function Action({label,onClick}:{label:string;onClick:()=>void}){return <button onClick={onClick} className="min-h-10 rounded-xl border border-[#333] px-3 text-xs font-bold">{label}</button>}
