"use client";
import Link from "next/link";
import { useMemo, useState } from "react";
import { useWorkouts } from "@/components/workouts/WorkoutProvider";
import { adherenceSummary, workoutStreak } from "@/lib/workouts/progress";

export default function ClientWorkoutReview({clientId}:{clientId:string}){const{snapshot,saveCoachNote,getExercise}=useWorkouts();const[note,setNote]=useState("");const[saved,setSaved]=useState(false);const assignments=snapshot.assignments.filter((item)=>item.clientId===clientId);const active=assignments.find((item)=>item.status==="active");const moves=useMemo(()=>snapshot.scheduleChanges.filter((item)=>item.clientId===clientId).sort((a,b)=>b.movedAt.localeCompare(a.movedAt)),[clientId,snapshot.scheduleChanges]);const history=useMemo(()=>[...snapshot.completedWorkouts].filter((item)=>item.clientId===clientId).sort((a,b)=>b.completedAt.localeCompare(a.completedAt)),[clientId,snapshot.completedWorkouts]);const weekly=adherenceSummary(snapshot.completedWorkouts,active,new Date());const submit=async()=>{if(!note.trim())return;const ok=await saveCoachNote({id:`coach-note-${Date.now()}`,coachId:"",clientId,body:note.trim(),createdAt:new Date().toISOString()});if(ok){setNote("");setSaved(true)}};return <main className="px-4 py-8 text-white"><div className="mx-auto max-w-6xl"><p className="text-xs font-bold text-[#D4AF37]">סקירת פעילות לקוח</p><h1 className="mt-2 text-3xl font-black">אימונים וביצועים</h1>{active?<section className="mt-5 rounded-[24px] border border-[#3A321B] bg-[#17150F] p-5"><h2 className="text-xl font-black">הקצאה פעילה</h2><p className="mt-2 text-sm text-zinc-400">{snapshot.programs.find((item)=>item.id===active.programId)?.name??"תוכנית לא זמינה"} · {active.weeklyFrequency} אימונים בשבוע</p></section>:<p className="mt-5 text-zinc-500">אין הקצאה פעילה.</p>}<dl className="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-3"><Info label="היענות שבועית" value={`${weekly.completed}/${weekly.expected} · ${weekly.percent}%`}/><Info label="חסרים השבוע" value={String(weekly.missed)}/><Info label="רצף" value={`${workoutStreak(snapshot.completedWorkouts,clientId)} אימונים`}/></dl><section className="mt-6 rounded-[22px] border border-[#D4AF37]/20 bg-[#17150F] p-5"><h2 className="text-xl font-black">אימונים שהוזזו</h2>{moves.length?<div className="mt-3 space-y-3">{moves.map((move)=>{const program=snapshot.programs.find((item)=>item.id===move.programId);const day=program?.days.find((item)=>item.id===move.dayId);return <article key={move.id} className="rounded-xl border border-[#3A321B] p-4"><strong>{day?.name??"אימון"}</strong><p className="mt-2 text-sm text-zinc-400">מקורי: {new Date(`${move.originalDate}T00:00:00`).toLocaleDateString("he-IL",{timeZone:"Asia/Jerusalem"})} · חדש: {new Date(`${move.scheduledDate}T00:00:00`).toLocaleDateString("he-IL",{timeZone:"Asia/Jerusalem"})}</p><p className="mt-1 text-xs text-zinc-600">הוזז: {new Date(move.movedAt).toLocaleString("he-IL",{timeZone:"Asia/Jerusalem"})}</p></article>})}</div>:<p className="mt-3 text-sm text-zinc-500">לא הוזזו אימונים.</p>}</section><section className="mt-6"><h2 className="text-2xl font-black">אימונים אחרונים</h2>{history.length?<div className="mt-3 space-y-3">{history.map((workout)=><article key={workout.id} className="rounded-[22px] border border-[#292929] bg-[#151515] p-5"><div className="flex justify-between gap-3"><strong>{snapshot.programs.find((item)=>item.id===workout.programId)?.days.find((day)=>day.id===workout.dayId)?.name??"אימון"}</strong><span className="text-sm text-zinc-500">{new Date(workout.completedAt).toLocaleDateString("he-IL",{timeZone:"Asia/Jerusalem"})}</span></div><p className="mt-2 text-sm text-zinc-500">{workout.exerciseResults.map((result)=>getExercise(result.exerciseId)?.name??"תרגיל").join(" · ")}</p><Link href={`/workouts/history/${workout.id}`} className="mt-3 inline-flex text-sm text-[#E7C85D]">פרטי אימון</Link></article>)}</div>:<p className="mt-3 text-zinc-500">אין אימונים שהושלמו.</p>}</section><section className="mt-6 rounded-[22px] border border-[#292929] bg-[#151515] p-5"><h2 className="text-xl font-black">הערת מאמן</h2><textarea className="nutrition-input mt-3 min-h-24" value={note} onChange={(event)=>{setNote(event.target.value);setSaved(false)}}/><button disabled={!note.trim()} onClick={submit} className="mt-3 min-h-12 rounded-xl bg-[#D4AF37] px-5 font-black text-black disabled:opacity-40">שמירת הערה</button>{saved&&<p className="mt-2 text-sm text-emerald-300">ההערה נשמרה ב-Supabase.</p>}</section></div></main>}
function Info({label,value}:{label:string;value:string}){return <div className="rounded-xl border border-[#292929] bg-[#151515] p-4"><span className="text-xs text-zinc-500">{label}</span><strong className="mt-1 block">{value}</strong></div>}
