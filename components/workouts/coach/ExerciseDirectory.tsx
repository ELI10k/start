"use client";
import Link from "next/link";
import { useMemo, useState } from "react";
import { Search } from "lucide-react";
import { queryExercises } from "@/lib/workouts/repository";
import { useWorkouts } from "@/components/workouts/WorkoutProvider";
import ExerciseGuidanceButton from "@/components/workouts/ExerciseGuidanceButton";
export default function ExerciseDirectory(){const{snapshot}=useWorkouts();const[search,setSearch]=useState("");const[muscle,setMuscle]=useState("");const[equipment,setEquipment]=useState("");const[category,setCategory]=useState("");const[difficulty,setDifficulty]=useState("");const all=snapshot.exercises;const unique=(values:(string|undefined)[])=>[...new Set(values.filter((value):value is string=>Boolean(value)))].sort((a,b)=>a.localeCompare(b,"he"));const results=useMemo(()=>queryExercises(all,{search,muscleGroup:muscle,equipment,category,difficulty,status:"active"}),[all,category,difficulty,equipment,muscle,search]);return <><section className="grid gap-3 rounded-[22px] border border-[#E5E7E5] bg-[#FFFFFF] p-4 sm:grid-cols-2 lg:grid-cols-5"><label className="relative sm:col-span-2 lg:col-span-5"><Search className="absolute right-4 top-4 text-[#16A34A]" size={18}/><span className="sr-only">חיפוש תרגיל</span><input className="nutrition-input pr-11" value={search} onChange={(event)=>setSearch(event.target.value)} placeholder="חיפוש בעברית"/></label><Filter label="שריר" value={muscle} onChange={setMuscle} values={unique(all.flatMap((item)=>[item.primaryMuscleGroup,...item.secondaryMuscleGroups]))}/><Filter label="ציוד" value={equipment} onChange={setEquipment} values={unique(all.map((item)=>item.equipment))}/><Filter label="קטגוריה" value={category} onChange={setCategory} values={unique(all.map((item)=>item.category))}/><Filter label="רמה" value={difficulty} onChange={setDifficulty} values={unique(all.map((item)=>item.difficulty))}/></section><p className="mt-4 text-xs text-[#5B5F5B]">{results.length} מתוך {all.length} תרגילים מאושרים</p>{results.length?<div className="mt-3 grid gap-3 md:grid-cols-2">{results.map((exercise)=><article key={exercise.id} className="rounded-[22px] border border-[#E5E7E5] bg-[#FFFFFF] p-5"><div className="flex justify-between gap-3"><div><h2 className="text-lg font-black">{exercise.name}</h2><p className="mt-1 text-xs text-[#5B5F5B]">{[exercise.primaryMuscleGroup,exercise.equipment,exercise.category,exercise.difficulty].filter(Boolean).join(" · ")}</p></div><span className="text-xs text-[#16A34A]">{exercise.status==="active"?"פעיל":"בארכיון"}</span></div><p className="mt-3 line-clamp-2 text-sm text-[#5B5F5B]">{exercise.executionNotes??"לא סופקו הערות ביצוע"}</p><div className="mt-3 flex flex-wrap items-center gap-4"><Link href={`/workouts/exercises/${exercise.id}`} className="inline-flex min-h-11 items-center text-sm text-[#16A34A]">פרטים ווידאו</Link><ExerciseGuidanceButton exercise={exercise} variant="link"/>{!exercise.cues.length&&!exercise.commonMistakes.length&&<span className="pill">ללא דגשים</span>}</div><p className="text-[10px] text-[#3F433F]">מקור: {exercise.sourceWorkbooks.join(", ")}</p></article>)}</div>:<div className="mt-4 rounded-[22px] border border-dashed border-[#E5E7E5] p-10 text-center text-[#5B5F5B]">{all.length?"לא נמצאו תרגילים התואמים לסינון.":"קובצי בנק התרגילים המאושרים אינם נמצאים במאגר ולכן לא הומצאו תרגילים חלופיים."}</div>}</>}
function Filter({label,value,onChange,values}:{label:string;value:string;onChange:(value:string)=>void;values:string[]}){return <label className="text-xs text-[#5B5F5B]">{label}<select className="nutrition-input mt-1" value={value} onChange={(event)=>onChange(event.target.value)}><option value="">הכול</option>{values.map((item)=><option key={item} value={item}>{item}</option>)}</select></label>}
