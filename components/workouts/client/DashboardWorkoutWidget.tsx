"use client";
import Link from "next/link";
import { useWorkouts } from "@/components/workouts/WorkoutProvider";
import { activeAssignmentFor, getTodayWorkoutDay } from "@/lib/workouts/progress";
export default function DashboardWorkoutWidget(){const{snapshot,currentClientId,loading,persistenceError}=useWorkouts();const assignment=activeAssignmentFor(snapshot.assignments,currentClientId,new Date().toISOString().slice(0,10));const program=snapshot.programs.find((item)=>item.id===assignment?.programId);const today=program?getTodayWorkoutDay(program,snapshot.completedWorkouts,currentClientId):undefined;const active=snapshot.activeSessions.find((item)=>item.clientId===currentClientId);return <section className="mt-6 rounded-[26px] border border-[#3A321B] bg-[#17150F] p-5"><h2 className="text-xl font-black">האימון הבא</h2>{loading?<p role="status" className="mt-3 text-sm text-zinc-500">טוענים את תוכנית האימון…</p>:persistenceError?<p role="alert" className="mt-3 text-sm text-red-300">{persistenceError}</p>:program?<><p className="mt-2 text-zinc-400">{program.name}{today?` · ${today.name}`:" · ימי האימון זמינים במסך האימונים"}</p><Link href={active?`/workouts/${active.programId}/${active.dayId}`:"/workouts"} className="mt-4 inline-flex min-h-12 items-center rounded-2xl bg-[#D4AF37] px-5 font-black text-black">{active?"המשך אימון פעיל":"למסך האימונים"}</Link></>:<p className="mt-3 text-sm text-zinc-500">אין כרגע תוכנית אימונים פעילה.</p>}</section>}
