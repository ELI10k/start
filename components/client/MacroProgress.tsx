export default function MacroProgress({ label, current, target, unit = "" }: { label: string; current: number; target?: number; unit?: string }) { const percent = target && target > 0 ? Math.max(0, current / target * 100) : 0; const over = !!target && current > target; return <div><div className="mb-2 flex items-center justify-between gap-3 text-sm"><span className="text-zinc-400">{label}</span><strong>{current} {unit}<span className="font-normal text-zinc-600"> / {target ?? "—"}</span></strong></div><div className="h-2.5 overflow-hidden rounded-full bg-zinc-800"><div className={`h-full rounded-full ${over ? "bg-red-500" : percent >= 100 ? "bg-emerald-500" : "bg-[#D4AF37]"}`} style={{ width: `${Math.min(100, percent)}%` }}/></div><p className={`mt-1.5 text-xs ${over ? "text-red-400" : "text-zinc-600"}`}>{target ? over ? `חריגה של ${Number((current-target).toFixed(1))} ${unit}` : `נותרו ${Number((target-current).toFixed(1))} ${unit}` : "לא הוגדר יעד"}</p></div>; }
