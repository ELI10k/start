export default function MacroProgress({ label, current, target, unit = "" }: { label: string; current: number; target?: number; unit?: string }) { const percent = target && target > 0 ? Math.max(0, current / target * 100) : 0; const over = !!target && current > target; return <div><div className="mb-2 flex items-center justify-between gap-3 text-sm"><span className="text-[#5B5F5B]">{label}</span><strong>{current} {unit}<span className="font-normal text-[#3F433F]"> / {target ?? "—"}</span></strong></div><div className="h-2.5 overflow-hidden rounded-full bg-[#F1F3F1]"><div className={`h-full rounded-full ${over ? "bg-[#DC2626]" : percent >= 100 ? "bg-[#16A34A]" : "bg-[#16A34A]"}`} style={{ width: `${Math.min(100, percent)}%` }}/></div><p className={`mt-1.5 text-xs ${over ? "text-[#DC2626]" : "text-[#3F433F]"}`}>{target ? over ? `חריגה של ${Number((current-target).toFixed(1))} ${unit}` : `נותרו ${Number((target-current).toFixed(1))} ${unit}` : "לא הוגדר יעד"}</p></div>; }
