"use client";
import { Check, RotateCcw } from "lucide-react";
import { useClientApp } from "./ClientAppProvider";
import { clientFoodMap, getMealTotals } from "@/lib/client-app";
import type { Meal } from "@/lib/meal-plans/types";
export default function ClientMealCard({ meal, compact = false }: { meal: Meal; compact?: boolean }) { const { mealStatuses, setMealStatus } = useClientApp(); const status = mealStatuses[meal.id] ?? "pending"; const totals = getMealTotals(meal); return <article className={`rounded-[24px] border p-5 transition ${status === "eaten" ? "border-emerald-500/30 bg-emerald-500/[.06]" : "border-[#292929] bg-[#151515]"}`}><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="text-xs font-bold text-[#D4AF37]">ארוחה {meal.order + 1}</p><h2 className="mt-1 text-xl font-black">{meal.name}</h2>{meal.note && <p className="mt-1 text-sm text-zinc-500">{meal.note}</p>}</div><span className="rounded-xl bg-black/25 px-3 py-2 text-sm font-bold">{totals.calories} קל׳ · {totals.protein} ג׳ חלבון</span></div>{!compact && <ul className="mt-4 divide-y divide-white/5">{meal.items.map((item) => { const food = clientFoodMap.get(item.foodId); if (!food) return null; const unit = food.servingLabel.includes("מ״ל") ? "מ״ל" : "גרם"; return <li key={item.id} className="flex items-center justify-between gap-4 py-3 text-sm"><span>{food.name}</span><strong className="shrink-0 text-[#E7C85D]">{item.quantityGrams} {unit}</strong></li>; })}</ul>}<div className="mt-4 flex items-center justify-between border-t border-white/5 pt-4"><span className={`text-xs font-bold ${status === "eaten" ? "text-emerald-400" : "text-zinc-500"}`}>{status === "eaten" ? "סומנה כהושלמה" : "טרם הושלמה"}</span><button type="button" onClick={() => setMealStatus(meal.id, status === "eaten" ? "pending" : "eaten")} aria-pressed={status === "eaten"} className={`flex min-h-11 items-center gap-2 rounded-2xl px-4 text-sm font-bold ${status === "eaten" ? "border border-[#333] text-zinc-300" : "bg-[#D4AF37] text-black"}`}>{status === "eaten" ? <RotateCcw size={16}/> : <Check size={17}/>} {status === "eaten" ? "ביטול סימון" : "סימון כהושלמה"}</button></div></article>; }
