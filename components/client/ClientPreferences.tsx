"use client";
import { Bell, LayoutList, Mail } from "lucide-react";
import { useClientApp } from "./ClientAppProvider";
export default function ClientPreferences() { const { preferences, updatePreferences } = useClientApp(); const rows = [{ key: "reminders" as const, label: "תזכורות יומיות", description: "מתג תצוגה בלבד; אין שירות הודעות פעיל.", icon: Bell }, { key: "weeklySummary" as const, label: "סיכום שבועי", description: "העדפה מקומית; לא נשלח דוא״ל.", icon: Mail }, { key: "compactMeals" as const, label: "ארוחות בתצוגה מצומצמת", description: "מצמצם את כרטיסי הארוחות במסך התזונה.", icon: LayoutList }]; return <div className="space-y-3">{rows.map(({ key, label, description, icon: Icon }) => <label key={key} className="flex min-h-20 items-center gap-4 rounded-[22px] border border-[#E5E7E5] bg-[#FFFFFF] p-4"><Icon className="shrink-0 text-[#16A34A]"/><span className="min-w-0 flex-1"><strong className="block">{label}</strong><span className="mt-1 block text-xs text-[#5B5F5B]">{description}</span></span><input aria-label={label} type="checkbox" className="size-6 shrink-0 accent-[#16A34A]" checked={preferences[key]} onChange={(event) => updatePreferences({ ...preferences, [key]: event.target.checked })}/></label>)}</div>; }
