import Link from "next/link";
export default function PageHeader({ eyebrow, title, description, action }: { eyebrow: string; title: string; description?: string; action?: { href: string; label: string } }) { return <header className="mb-8 flex flex-wrap items-end justify-between gap-4 border-b border-white/5 pb-6"><div><p className="text-xs font-black tracking-[.18em] text-[#D4AF37]">{eyebrow}</p><h1 className="mt-2 text-3xl font-black tracking-tight sm:text-4xl">{title}</h1>{description && <p className="mt-2 max-w-xl text-sm leading-6 text-zinc-400">{description}</p>}</div>{action && <Link href={action.href} className="start-action min-h-11 rounded-2xl border border-[#D4AF37]/40 px-4 py-2 text-sm font-bold text-[#E7C85D]">{action.label}</Link>}</header>; }
