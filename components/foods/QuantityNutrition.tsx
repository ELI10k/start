"use client";
import { useState } from "react";
import type { Food } from "@/lib/foods";
import { calculateFoodNutrition } from "@/lib/meal-plans/calculations";
export default function QuantityNutrition({ food }: { food: Food }) { const [quantity, setQuantity] = useState(100); const totals = calculateFoodNutrition(food, quantity); return <section className="border-t border-[#E5E7E5] p-5 sm:p-8"><label className="block max-w-xs text-sm font-bold">כמות לחישוב (גרם)<input aria-describedby="quantity-help" className="nutrition-input mt-2" type="number" min="0.1" max="5000" step="0.1" value={quantity} onChange={(event) => setQuantity(Math.max(0, Number(event.target.value)))}/></label><p id="quantity-help" className="mt-2 text-xs text-[#5B5F5B]">חישוב דמו לינארי לפי הערכים ל־100 גרם במאגר.</p><div className="mt-4 grid grid-cols-2 gap-3 sm:grid-cols-4"><Value label="קלוריות" value={totals.calories}/><Value label="חלבון" value={totals.protein}/><Value label="פחמימות" value={totals.carbs}/><Value label="שומן" value={totals.fat}/></div></section>; }
function Value({ label, value }: { label: string; value: number }) { return <div className="rounded-2xl bg-[#F7F8F7] p-3"><span className="text-xs text-[#5B5F5B]">{label}</span><strong className="mt-1 block">{value}</strong></div>; }
