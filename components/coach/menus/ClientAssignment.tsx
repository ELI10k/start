"use client";
import { clients } from "@/lib/clients";
export default function ClientAssignment({ value, onChange }: { value?: string; onChange: (id?: string) => void }) { return <label className="block text-sm font-bold text-[#3F433F]">שיוך ללקוח<select className="nutrition-input mt-2" value={value ?? ""} onChange={(event) => { const next = clients.some((client) => client.id === event.target.value) ? event.target.value : undefined; if (value && next && value !== next && !window.confirm("החלפת הלקוח תסיר את השיוך הנוכחי לאחר שמירת התפריט. להמשיך?")) return; onChange(next); }}><option value="">ללא שיוך</option>{clients.map((client) => <option key={client.id} value={client.id}>{client.fullName}</option>)}</select><span className="mt-2 block text-xs font-normal text-[#3F433F]">במערכת הדמו השיוך נשמר בסשן המאמן בלבד.</span></label>; }
