"use client";
import { Search, SlidersHorizontal, Star, X } from "lucide-react";
import { useDeferredValue, useMemo, useState, useTransition } from "react";
import type { Food, FoodSort } from "@/lib/foods";
import { ALL_CATEGORIES, queryFoods } from "@/lib/foods/repository";
import{toggleFoodFavorite}from"@/app/actions/food-favorites";
import{foodMacroGroup,type MacroGroup}from"@/lib/nutrition/food-groups";

export default function FoodDatabase({ foods,initialFavorites=[] }: { foods: readonly Food[];initialFavorites?:readonly string[] }) {
  const [search, setSearch] = useState(""); const [category, setCategory] = useState(ALL_CATEGORIES); const [sort, setSort] = useState<FoodSort>("relevant"); const deferredSearch = useDeferredValue(search);const[favorites,setFavorites]=useState(()=>new Set(initialFavorites));const[favoriteOnly,setFavoriteOnly]=useState(false);const[,startTransition]=useTransition();
  const categories = useMemo(() => [ALL_CATEGORIES, ...new Set(foods.map((food) => food.category))].sort((a, b) => a === ALL_CATEGORIES ? -1 : b === ALL_CATEGORIES ? 1 : a.localeCompare(b, "he")), [foods]);
  const visible = useMemo(() => {const queried=queryFoods(foods,{search:deferredSearch,category,sort});return favoriteOnly?queried.filter((food)=>favorites.has(food.id)):queried}, [foods, deferredSearch, category, sort,favoriteOnly,favorites]);
  const favoriteGroups=useMemo(()=>{const groups:Record<MacroGroup,Food[]>={protein:[],carbohydrate:[],fat:[]};for(const food of visible)groups[foodMacroGroup({...food,protein:food.protein??null,carbs:food.carbs??null,fat:food.fat??null})].push(food);return groups},[visible]);
  const clear = () => { setSearch(""); setCategory(ALL_CATEGORIES); setSort("relevant");setFavoriteOnly(false) };
  const toggle=(foodId:string)=>{const was=favorites.has(foodId);setFavorites((current)=>{const next=new Set(current);if(was)next.delete(foodId);else next.add(foodId);return next});startTransition(async()=>{try{const result=await toggleFoodFavorite(foodId,!was);setFavorites((current)=>{const next=new Set(current);if(result.favorite)next.add(foodId);else next.delete(foodId);return next})}catch{setFavorites((current)=>{const next=new Set(current);if(was)next.add(foodId);else next.delete(foodId);return next})}})};
  return <section className="food-database" aria-busy={search !== deferredSearch}><header className="food-database__header"><div><span className="food-database__eyebrow">START FOOD DATABASE</span><h1 className="food-database__title">מאגר מזונות</h1><p className="food-database__description">חיפוש לפי שם, מותג, קטגוריה או ברקוד</p><p className="mt-3 text-xs text-[#5B5F5B]">{foods.length} מוצרים מהמאגר הרשמי</p></div><div className="food-database__count"><strong>{visible.length}</strong><span>תוצאות</span></div></header><div className="grid gap-3 rounded-[22px] border border-[#E5E7E5] bg-[#FFFFFF] p-3 sm:grid-cols-[minmax(0,1fr)_220px]"><label className="relative"><span className="sr-only">חיפוש מזונות</span><Search className="absolute right-4 top-4 text-[#16A34A]" size={18}/><input className="nutrition-input pr-11" type="search" value={search} onChange={(event) => setSearch(event.target.value)} placeholder="חיפוש מזון..." autoComplete="off"/>{search && <button type="button" onClick={() => setSearch("")} aria-label="ניקוי חיפוש" className="absolute left-3 top-3 rounded-xl p-2 text-[#5B5F5B] hover:text-[#0B0B0B]"><X size={18}/></button>}</label><label className="flex items-center gap-2 text-xs text-[#5B5F5B]"><SlidersHorizontal size={17}/>מיון<select className="nutrition-input" value={sort} onChange={(event) => setSort(event.target.value as FoodSort)}><option value="relevant">רלוונטיות</option><option value="protein-high">חלבון: גבוה לנמוך</option><option value="calories-low">קלוריות: נמוך לגבוה</option><option value="calories-high">קלוריות: גבוה לנמוך</option><option value="alphabetical">א׳–ת׳</option></select></label></div><div className="food-categories" aria-label="סינון לפי קטגוריה"><button type="button" aria-pressed={favoriteOnly} className={favoriteOnly?"food-category active":"food-category"} onClick={()=>setFavoriteOnly((value)=>!value)}><Star size={15} fill={favoriteOnly?"currentColor":"none"}/>מועדפים</button>{categories.map((item) => <button key={item} type="button" aria-pressed={category === item} className={category === item ? "food-category active" : "food-category"} onClick={() => setCategory(item)}>{item}</button>)}</div>{visible.length ? favoriteOnly?<div className="space-y-8">{(["protein","carbohydrate","fat"]as const).map((group)=>favoriteGroups[group].length?<section key={group}><h2 className="mb-3 text-xl font-black">{group==="protein"?"חלבונים":group==="carbohydrate"?"פחמימות":"שומנים"}</h2><div className="food-list">{favoriteGroups[group].map((food)=><FoodCard key={food.id} food={food} favorite onToggle={()=>toggle(food.id)}/>)}</div></section>:null)}</div>:<div className="food-list">{visible.map((food) => <FoodCard key={food.id} food={food} favorite={favorites.has(food.id)} onToggle={()=>toggle(food.id)}/>)}</div> : <div className="food-empty-state"><Search className="mx-auto mb-3 text-[#3F433F]" size={38}/><h2>לא נמצאו מוצרים</h2><p>אפשר לנסות שם, מותג, ברקוד או קטגוריה אחרים.</p><button type="button" onClick={clear}>ניקוי הסינונים</button></div>}</section>;
}
export function FoodCard({ food,favorite=false,onToggle }: { food: Food;favorite?:boolean;onToggle?:()=>void }) { return <article className="food-card"><div className="food-card__top"><div><span className="food-card__category">{food.category}</span><h2 className="food-card__name">{food.name}</h2>{food.brand && <p className="food-card__brand">{food.brand}</p>}</div><div className="flex items-start gap-2"><button type="button" onClick={onToggle} aria-label={favorite?`הסרת ${food.name} מהמועדפים`:`הוספת ${food.name} למועדפים`} aria-pressed={favorite} className={`inline-flex size-11 shrink-0 items-center justify-center rounded-full border ${favorite?"border-[#16A34A] bg-[#ECFDF3] text-[#16A34A]":"border-[#D9DDD9] bg-white text-[#5B5F5B]"}`}><Star size={20} fill={favorite?"currentColor":"none"}/></button><div className="min-w-24 rounded-2xl border border-[#B7E4C7] bg-[#ECFDF3] px-4 py-3 text-center text-[#15803D]"><strong className="block text-2xl leading-none">{food.calories}</strong><span className="mt-1 block text-xs">קלוריות</span></div></div></div><div className="food-card__macros"><Macro label="חלבון" value={food.protein}/><Macro label="פחמימה" value={food.carbs}/><Macro label="שומן" value={food.fat}/></div><div className="food-card__footer"><span>{food.servingLabel}</span></div></article>; }
function Macro({ label, value }: { label: string; value?: number }) { return <div className="food-macro"><span>{label}</span><strong>{value === undefined ? "—" : `${value} גרם`}</strong></div>; }
