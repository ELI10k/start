"use client";
import { useEffect } from "react";
export default function FoodsError({ error, unstable_retry }: { error: Error & { digest?: string }; unstable_retry: () => void }) { useEffect(() => { console.error(error); }, [error]); return <main className="min-h-screen bg-[#FFFFFF] px-4 py-24 text-center text-[#0B0B0B]"><h1 className="text-3xl font-black">לא הצלחנו לטעון את מאגר המזונות</h1><p className="mt-3 text-[#5B5F5B]">אפשר לנסות לטעון את הנתונים מחדש.</p><button type="button" onClick={() => unstable_retry()} className="mt-6 rounded-2xl bg-[#16A34A] px-5 py-3 font-black text-[#FFFFFF]">ניסיון נוסף</button></main>; }
