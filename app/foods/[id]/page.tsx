import Link from "next/link";
import { notFound } from "next/navigation";
import { createSupabaseServerClient } from "@/lib/supabase/server";
export default async function FoodDetailsPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const supabase=await createSupabaseServerClient();const{data:food,error}=await supabase.from("foods").select("*").eq("id",id).maybeSingle();if(error)throw error;if(!food)notFound();return <main className="min-h-screen bg-[#090909] px-4 py-8 text-white"><article className="mx-auto max-w-3xl rounded-[28px] border border-[#3A321B] bg-[#151515] p-6"><Link href="/foods" className="text-sm text-[#D4AF37]">חזרה למאגר</Link><p className="mt-6 text-xs text-[#D4AF37]">{food.category}</p><h1 className="mt-2 text-3xl font-black">{food.name}</h1><p className="mt-2 text-zinc-500">{food.brand} · ערכים ל־100 גרם</p><dl className="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4"><Value label="קלוריות" value={food.calories}/><Value label="חלבון" value={food.protein}/><Value label="פחמימות" value={food.carbs}/><Value label="שומן" value={food.fat}/></dl><p className="mt-5 text-sm text-zinc-500">מנה: {food.serving_label}. ערכים חסרים נשארים לא זמינים ואינם מוערכים.</p></article></main>}
function Value({label,value}:{label:string;value:unknown}){return <div className="rounded-2xl bg-black/25 p-4"><dt className="text-xs text-zinc-500">{label}</dt><dd className="mt-1 text-xl font-black">{value===null?"לא זמין":String(value)}</dd></div>}
