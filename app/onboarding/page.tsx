import { redirect } from "next/navigation";
import { completeClientOnboarding } from "@/app/actions/onboarding";
import { getAuthContext } from "@/lib/data/product-repository";
import { createSupabaseServerClient } from "@/lib/supabase/server";

export default async function Onboarding(){const auth=await getAuthContext();if(!auth)redirect("/login");if(auth.role!=="client")redirect("/coach");const supabase=await createSupabaseServerClient();const {data:relationship}=await supabase.from("coach_client_relationships").select("coach_id").eq("client_id",auth.id).eq("status","active").maybeSingle();if(relationship)redirect("/");return <main className="px-4 py-8 sm:px-6"><div className="mx-auto max-w-3xl"><form action="/auth/logout" method="post" className="flex justify-end"><button className="min-h-11 text-sm text-zinc-400">התנתקות</button></form><form action={completeClientOnboarding}><p className="text-xs font-black tracking-[.2em] text-[#D4AF37]">START</p><h1 className="mt-2 text-3xl font-black">כמה פרטים לפני שמתחילים</h1><p className="mt-2 text-zinc-400">המידע נשמר רק עבורך ועבור המאמן המשויך אליך.</p><section className="mt-6 grid gap-4 rounded-[28px] border border-[#292929] bg-[#151515] p-5 sm:grid-cols-2"><Field label="מטרה" name="goal"/><Field label="יעד משקל" name="targetWeight" type="number" step="0.1"/><Field label="משקל נוכחי" name="weight" type="number" step="0.1"/><Field label="גובה" name="height" type="number"/><Field label="תאריך לידה" name="birthDate" type="date"/><Field label="רמת פעילות" name="activityLevel"/><Field label="העדפות תזונה" name="dietaryPreferences"/><Field label="מאכלים שלא אוהב" name="foodDislikes"/><Field label="אלרגיות או מגבלות" name="allergies"/><Field label="שעות אכילה" name="mealTimes"/><Field label="ניסיון באימונים" name="trainingExperience"/><Field label="מיקום אימונים" name="trainingLocation"/><Field label="ציוד זמין" name="equipment"/><Field label="אימונים בשבוע" name="weeklyWorkouts" type="number" min="1" max="14"/><Field label="ימים מועדפים" name="preferredDays"/><Field label="תזכורות רצויות" name="reminders"/><Field label="היקף מותניים" name="waist" type="number" step="0.1"/><label className="sm:col-span-2">הערות רפואיות<textarea name="medicalNotes" className="nutrition-input mt-2 min-h-24"/></label></section><label className="mt-5 flex gap-3 text-sm"><input required name="terms" type="checkbox"/>אני מאשר/ת את תנאי השימוש ושמירת המידע לצורך הליווי.</label><button className="mt-6 min-h-14 w-full rounded-2xl bg-[#D4AF37] px-6 font-black text-black">שמירה והמשך</button></form></div></main>}
function Field({label,name,type="text",...props}:{label:string;name:string;type?:string;[key:string]:unknown}){return <label className="block text-sm font-bold">{label}<input name={name} type={type} className="nutrition-input mt-2" {...props}/></label>}
