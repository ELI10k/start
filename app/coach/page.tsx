import Link from "next/link";
import { redirect } from "next/navigation";
import { getAuthContext, getCoachCheckInDashboard, listCoachClients, listCoachMenus } from "@/lib/data/product-repository";
import DashboardWorkoutActivity from "@/components/workouts/coach/DashboardWorkoutActivity";
import { getUnreadNotificationCount } from "@/lib/notifications/repository";
export default async function CoachDashboard(){const auth=await getAuthContext();if(!auth)redirect("/login");if(auth.role!=="coach")redirect("/unauthorized");const[clients,menus,unreadNotifications,checkIns]=await Promise.all([listCoachClients(auth.id),listCoachMenus(auth.id),getUnreadNotificationCount(),getCoachCheckInDashboard(auth.id)]);return <main className="px-4 py-10 sm:px-6 lg:px-8"><div className="mx-auto max-w-7xl"><header className="border-b border-white/5 pb-7"><p className="text-xs font-black tracking-[.2em] text-[#D4AF37]">START COACH</p><h1 className="mt-3 text-3xl font-black tracking-tight sm:text-4xl">שלום, {auth.fullName.split(" ")[0]}</h1><p className="mt-2 text-zinc-400">תצוגה ברורה של הפעילות העסקית שלך.</p></header><section className="mt-7 grid grid-cols-2 gap-3 sm:grid-cols-5"><Metric label="לקוחות פעילים" value={clients.length}/><Metric label="תפריטים" value={menus.length}/><Metric label="צ׳ק־אינים חדשים" value={checkIns.newCount}/><Metric label="ממתינים לטיפול" value={checkIns.newCount+checkIns.respondedCount}/><Metric label="התראות פתוחות" value={unreadNotifications}/></section><section className="mt-6"><h2 className="sr-only">פעולות מהירות</h2><div className="grid grid-cols-2 gap-3 sm:grid-cols-4 xl:grid-cols-8"><Quick href="/coach/clients/new" label="לקוח חדש" primary/><Quick href="/coach/clients" label="כל הלקוחות"/><Quick href="/coach/check-ins?status=new" label="צ׳ק־אינים חדשים"/><Quick href="/coach/menus/new" label="יצירת תפריט"/><Quick href="/coach/menus" label="כל התפריטים"/><Quick href="/coach/workouts/new" label="תוכנית אימון חדשה"/><Quick href="/coach/content/new" label="תוכן חדש"/><Quick href="/coach/notifications" label="התראות"/></div></section><section className="mt-8 rounded-[26px] border border-[#3A321B] bg-[#17150F] p-5"><div className="flex items-center justify-between gap-4"><div><h2 className="text-xl font-black">צ׳ק־אינים אחרונים</h2><p className="mt-1 text-xs text-zinc-500">עדכונים חדשים דורשים מעבר ותגובה.</p></div><Link href="/coach/check-ins" className="text-sm font-bold text-[#D4AF37]">לכל הצ׳ק־אינים</Link></div>{checkIns.recent.length?<div className="mt-4 grid gap-2">{checkIns.recent.map(item=><Link key={item.id} href={`/coach/check-ins#check-in-${item.id}`} className="flex flex-wrap items-center justify-between gap-3 rounded-xl bg-black/25 p-3 text-sm"><span><strong>{item.client?.full_name??"לקוח"}</strong><span className="mr-2 text-zinc-500">{new Date(item.submitted_at).toLocaleDateString("he-IL",{timeZone:"Asia/Jerusalem"})}</span></span><span className={item.handled_at?"text-emerald-300":item.status==="reviewed"?"text-sky-300":"text-amber-300"}>{item.handled_at?"טופל":item.status==="reviewed"?"נענתה":"חדש"}</span></Link>)}</div>:<p className="mt-4 rounded-xl border border-dashed border-[#333] p-8 text-center text-zinc-500">אין צ׳ק־אינים להצגה.</p>}</section><DashboardWorkoutActivity/></div></main>}
function Metric({label,value}:{label:string;value:number}){return <div className="start-surface min-h-32 rounded-[22px] p-5"><strong className="text-3xl">{value}</strong><span className="mt-2 block text-sm text-zinc-500">{label}</span></div>};function Quick({href,label,primary=false}:{href:string;label:string;primary?:boolean}){return <Link href={href} className={primary?"start-action flex min-h-14 items-center rounded-2xl bg-[#D4AF37] px-5 font-black text-black hover:bg-[#E7C85D]":"start-action flex min-h-14 items-center rounded-2xl border border-[#3A321B] bg-[#17150F] px-5 font-black text-[#D4AF37]"}>{label}</Link>}
