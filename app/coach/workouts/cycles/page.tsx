import{redirect}from"next/navigation";import Link from"next/link";
import{getAuthContext}from"@/lib/data/product-repository";import{createSupabaseServerClient}from"@/lib/supabase/server";import{reviewWorkoutCycle}from"@/app/actions/workout-cycles";
type Change={from?:string;to?:string;reason?:string};type Proposal={id:string;cycle_start:string;cycle_end:string;completed_workouts:number;expected_workouts:number;completion_percent:number;status:string;changes:Change[]|null;profiles:{full_name:string|null}|null;workout_programs:{name:string|null}|null};
export default async function Page(){const auth=await getAuthContext();if(!auth)redirect("/login");if(auth.role!=="coach")redirect("/unauthorized");const s=await createSupabaseServerClient();const{data}=await s.from("workout_cycle_proposals").select("id,client_id,cycle_start,cycle_end,completed_workouts,expected_workouts,completion_percent,status,changes,created_at,profiles!workout_cycle_proposals_client_id_fkey(full_name),workout_programs!workout_cycle_proposals_current_program_id_fkey(name)").order("created_at",{ascending:false});const proposals=(data??[])as unknown as Proposal[];return <main className="px-4 py-8 sm:px-6"><div className="mx-auto max-w-5xl"><Link href="/coach/workouts" className="text-sm font-bold text-[#16A34A]">חזרה לתוכניות</Link><h1 className="mt-3 text-3xl font-black">הצעות לתוכנית הבאה</h1><p className="mt-2 text-[#5B5F5B]">ההצעות גלויות למאמן בלבד. הלקוח ממשיך בתוכנית הקיימת עד אישור ושליחה.</p>{!proposals.length?<section className="mt-6 rounded-[24px] border border-dashed border-[#C9CDC9] bg-white p-10 text-center"><h2 className="text-xl font-black">אין כרגע לקוחות שהשלימו מחזור</h2><p className="mt-2 text-sm text-[#5B5F5B]">הצעה תופיע לאחר ארבעה שבועות ולפחות 80% מהאימונים.</p></section>:<div className="mt-6 space-y-4">{proposals.map((p)=><article key={p.id} className="rounded-[24px] border border-[#E5E7E5] bg-white p-5"><div className="flex flex-wrap justify-between gap-3"><div><h2 className="text-xl font-black">{p.profiles?.full_name??"לקוח"}</h2><p className="text-sm text-[#5B5F5B]">{p.workout_programs?.name} · {p.cycle_start}–{p.cycle_end}</p></div><strong className="text-[#16A34A]">{p.completed_workouts}/{p.expected_workouts} · {p.completion_percent}%</strong></div><ul className="mt-4 space-y-2 text-sm">{(p.changes??[]).map((c,i)=><li key={i} className="rounded-xl bg-[#F7F8F7] p-3">חזרות: {c.from} ← {c.to} · {c.reason}</li>)}</ul>{p.status==="pending"?<form action={reviewWorkoutCycle} className="mt-4"><input type="hidden" name="id" value={p.id}/><textarea name="note" className="nutrition-input min-h-20" placeholder="הערת מאמן (רשות)"/><div className="mt-3 flex gap-2"><button name="decision" value="approve" className="premium-primary-button">אישור ושליחה ללקוח</button><button name="decision" value="reject" className="premium-secondary-button">דחיית ההצעה</button></div></form>:<span className="mt-4 inline-flex rounded-full border px-3 py-1 text-xs">{p.status==="approved"?"אושרה ונשלחה":"נדחתה"}</span>}</article>)}</div>}</div></main>}
