"use client";
import { useEffect } from "react";
import Link from "next/link";
export default function AppError({ error, unstable_retry }: { error: Error & { digest?: string }; unstable_retry: () => void }) { useEffect(() => { console.error(error); }, [error]); return <main className="min-h-screen px-4 py-24 text-center text-white"><section className="start-surface mx-auto max-w-lg rounded-[28px] p-8 sm:p-10"><p className="text-xs font-black tracking-[.2em] text-[#D4AF37]">START</p><h1 className="mt-3 text-3xl font-black">לא הצלחנו לטעון את המסך</h1><p className="mt-3 leading-7 text-zinc-400">אפשר לנסות שוב. הנתונים שלך לא השתנו.</p><div className="mt-7 flex flex-wrap justify-center gap-3"><button type="button" onClick={() => unstable_retry()} className="start-gold-button min-h-12 rounded-2xl bg-[#D4AF37] px-5 font-black text-black">ניסיון נוסף</button><Link href="/" className="start-action inline-flex min-h-12 items-center rounded-2xl border border-[#333] px-5 font-bold">מסך הבית</Link></div></section></main>; }
