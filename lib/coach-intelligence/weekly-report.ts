import { calculateCoachScores, weeklyRecommendations, type HabitMetrics } from "./rule-engine.ts";
export type WeeklyReportDraft=Readonly<{scores:ReturnType<typeof calculateCoachScores>;summary:string;strengths:readonly string[];improvements:readonly string[];actions:readonly string[];status:"ready"|"insufficient_data"}>;
export function generateWeeklyReport(metrics:HabitMetrics):WeeklyReportDraft{const sufficient=metrics.logins>0||metrics.activeDays>0||metrics.checkIns>0;if(!sufficient)return{scores:calculateCoachScores(metrics),summary:"לא נאספו מספיק נתונים השבוע.",strengths:[],improvements:[],actions:[],status:"insufficient_data"};const scores=calculateCoachScores(metrics);const strengths=[scores.workouts>=70?"התמדה באימונים":"" ,scores.nutrition>=70?"תיעוד תזונתי עקבי":""].filter(Boolean);const improvements=[scores.nutrition<70?"נדרש שיפור בתיעוד התזונה":"",scores.workouts<70?"נדרש שיפור בהשלמת אימונים":""].filter(Boolean);return{scores,summary:`ציון העקביות השבועי הוא ${scores.consistency}. ${improvements[0]??"ההתמדה השבועית טובה."}`,strengths,improvements,actions:weeklyRecommendations(metrics),status:"ready"}}
