export type Json = string | number | boolean | null | { [key: string]: Json | undefined } | Json[];
export type UserRole = "coach" | "client";
export type UserRoleRow = { user_id: string; role: UserRole; assigned_by: string | null; assigned_at: string; updated_at: string };
export type ProfileRow = { id: string; email: string; full_name: string; phone: string | null; role: UserRole; avatar_url: string | null; status: "active" | "paused" | "disabled"; is_test_account: boolean; created_at: string; updated_at: string };
export type ClientProfileRow = { user_id: string; goal: string | null; target_weight: number | null; height: number | null; birth_date: string | null; activity_level: string | null; calorie_target: number | null; protein_target: number | null; preferences: Json; notes: string | null };
export type FoodRow = { id: string; name: string; brand: string | null; category: string; calories: number; protein: number | null; carbs: number | null; fat: number | null; sugars: number | null; sodium_mg: number | null; calcium_mg: number | null; package_quantity: number | null; package_unit: string | null; barcode: string | null; serving_label: string; verification_status: string | null; notes: string | null; source_url: string | null; unit_weight_grams: number | null; calories_per_unit: number | null; units_per_package: number | null; search_text: string; created_at: string; updated_at: string };
export type MenuRow = { id: string; coach_id: string; client_id: string | null; title: string; description: string | null; status: "draft" | "published" | "active" | "archived"; calorie_target: number | null; protein_target: number | null; carbohydrate_target: number | null; fat_target: number | null; active_from: string | null; active_until: string | null; created_at: string; updated_at: string };
export type ProgressRow = { id: string; client_id: string; date: string; weight: number; waist: number | null; chest: number | null; hips: number | null; navel_circumference: number | null; notes: string | null; created_at: string };
export type CheckInRow = { id: string; client_id: string; submitted_at: string; adherence: number; hunger: number; energy: number; sleep: number; training: boolean; notes: string | null; coach_response: string | null; status: "submitted" | "reviewed"; created_at: string };
export type Database = { public: { Tables: Record<string, { Row: Record<string, unknown>; Insert: Record<string, unknown>; Update: Record<string, unknown>; Relationships: [] }>; Views: Record<string, never>; Functions: Record<string, { Args: Record<string, unknown>; Returns: unknown }>; Enums: Record<string, string>; CompositeTypes: Record<string, never> } };
